import {
	primaryColor
} from '../../../theme';

const presentationStyle = theme => ({
	container: {
		width: '100vw',
		height: '100vh',
		position: 'relative',
		backgroundColor: primaryColor
	}
});

export default presentationStyle;